初始化地图：m * n数组

箭头数据结构：
direction-上下左右
body-数组，存储箭头所占所有格子的坐标，首位表示头，顺序
color

关卡生成算法（基本思路由cursor给出，保证无互锁）
1. 头：在当前可放置区域的任意一格。
2. 方向：任选上下左右之一。
3. 出口带：从头的格子沿该方向延伸到矩形网格边界的整条射线，每一格都要属于当前可放置区域（即不能经过任何已占用的身体或出口带）。
4. 身体：从头向网格内生长，每一格都在当前可放置区域内，且与出口带、已有箭头身体/出口带不交。

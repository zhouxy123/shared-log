2.9
UI单位换算
rpx: 随屏幕宽度自适应缩放，设计稿中屏宽750px，等比例缩放
dp: 不同密度的屏幕上显示相似的尺寸

依赖问题解决：
首页库动态布局sdk：
homepage build.gradle中：
```gradle
api('com.meituan.android.dynamiclayout:library:12.53.200.0.1136129324-SNAPSHOT'){
    force = true
}
```


主工程动态布局sdk：
dependencies_base_component.gradle中：```
```gradle
implementation('com.meituan.android.dynamiclayout:library:12.53.200.0.1136129324-SNAPSHOT') {
    force = true
}
```

2.10
Android图片加载库Picasso工作流程：
```java
ImageView targetImageView;
Picasso.with(targetImageView.getContext())
	.load(url) // 图片url
	.transform() // 自定义变换，可返回处理后的bitmap
	.into(targetImageView) // 先把bitmap自动封装成PicassoDrawable，再进行绘制
```
